return {
    {
        'David-Kunz/gen.nvim',
        enabled = false,
        opts = {
            model = 'mistral', -- The default model to use.
            debug = false, -- Prints errors and the command which is run.
        },
    }
}
