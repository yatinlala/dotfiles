#!/usr/bin/bash -e

echo  "[a]wesome [i]3wm [s]way"

read input

if [ "$input" = "a" ]
then
    startx "$HOME/.config/X11/xinitrc.awesome"
elif [ "$input" = "i" ]
then
    startx "$HOME/.config/X11/xinitrc.i3"
else
    sway
fi
