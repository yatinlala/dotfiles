#Prefix is Ctrl-a
set -g prefix C-a
bind C-a send-prefix
unbind C-b
set -g status-style 'bg=#504945 fg=#ebdbb2'

#Mouse works as expected
set -g mouse on

set -g base-index 1

set -g mode-keys vi
set -g history-limit 10000

# y and p as in vim
bind Escape copy-mode
unbind p
bind p paste-buffer
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
bind-key -T copy-mode-vi 'Space' send -X halfpage-down
bind-key -T copy-mode-vi 'Bspace' send -X halfpage-up

# moving between panes with vim movement keys
bind -r ^ last-window
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# moving between windows with vim movement keys
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# resize panes with vim movement keys
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# bind -r D neww -c "#{pane_current_path}" "[[ -e TODO.md ]] && nvim TODO.md || nvim ~/dotfiles/todo.md"

# bind-key -r G run-shell "~/code/scripts/tmux-sessionizer ~/work/nrdp"
# bind-key -r C run-shell "~/code/scripts/tmux-sessionizer ~/work/tvui"
# bind-key -r R run-shell "~/code/scripts/tmux-sessionizer ~/work/milo"
# bind-key -r L run-shell "~/code/scripts/tmux-sessionizer ~/work/hpack"
# bind-key -r H run-shell "~/code/scripts/tmux-sessionizer ~/personal/vim-with-me"
# bind-key -r T run-shell "~/code/scripts/tmux-sessionizer ~/personal/harpoon"
# bind-key -r S run-shell "~/code/scripts/tmux-sessionizer ~/personal/crypto-legends"
# bind-key -r N run-shell "~/code/scripts/tmux-sessionizer ~/personal/the-hoff"
